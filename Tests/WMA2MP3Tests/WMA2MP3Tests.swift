import XCTest
@testable import WMA2MP3

/// Raccolta dei test di base del modulo principale.
final class WMA2MP3Tests: XCTestCase {
    /// Verifica minima usata per controllare che il target di test venga caricato.
    func testExample() throws {
        XCTAssertTrue(true)
    }
}
